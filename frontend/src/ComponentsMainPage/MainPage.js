import { Button } from '@mui/material';

function MainPage() {
    return (
        <div>
            <Button  href="#text-buttons">Доступные данные</Button>
            <Button>Создание фичи</Button>
        </div>
    );
}

export default MainPage;
